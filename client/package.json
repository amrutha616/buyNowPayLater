{
  "name": "bnpl-client-wrapper",
  "private": true,
  "scripts": {
    "dev": "npm --prefix ./frontend/frontend run dev -- --host",
    "build": "npm --prefix ./frontend/frontend run build",
    "preview": "npm --prefix ./frontend/frontend run preview"
  }
}
